# Phase 0 coverage matrix

Статус: planning evidence; неизвестное обозначено явно

| Area/mechanism | Phase 0 resolution/representation | Contract observables | Validation data in Phase 0 | Unknown or invalid outside range | Planned upgrade |
|---|---|---|---|---|---|
| causal topology | one feedback graph; L0–L9 as domains; timeline cross-cutting | causes, interval, digests, deltas, uncertainty, conservation, state hash | normative ADR/invariants + public-doc contract test | runtime dependency graph and scheduling | Phase 1 minimal graph; Phase 7 digital/institutional domains; Phase 8 optimized scheduler |
| mammalian mass accounting example | `CellCohort` ports only | total cell count/mass, oxygen amount | schema fixture + dimensional/conservation assertions | no tissue kinetics or clinical calibration | Phase 1 minimal mechanism, Phase 3 organ systems |
| organism coarse physiology (causal-kernel) | single thermal core + room surrogate reservoir; chemical store + digestion buffer; sleep phase with debt counter | exact energy conservation, core/ambient temperature projections (mK), metabolic power W, absorption flux uJ/s, sleep-debt seconds | `tests/parameter_realism.rs` bands derived from published values; 24h integration conservation; restart/replay parity | no renal/water balance, endocrine, O2/CO2 chain, substrate metabolism; passive thermal surrogate without effectors; circadian windows not two-process dynamics; Neko parameters fictional_assumption | Phase 3 organ systems; Phase 6 neurobiology; measured calibration per ADR-0014 |
| cell representation | cohort → `IndividualCellSet` example | count, mass, charge, O₂ amount, mean cell mass | deterministic lift/projection fixture, lineage and continuity assertions | cell cycle, mutation, spatial contacts | Phase 4 adaptive cohorts/individual lineages |
| neural representation | `NeuralPopulation` → `IndividualNeuronNetwork` example | neuron count, mean firing rate, total transmitter amount | deterministic lift/projection fixture and declared error bound | spikes, synapses, plasticity, regional calibration | Phase 6 replaceable neural implementations |
| Human morphotype | independent schema root | anatomy/binding/parameter references | minimal female Makise fixture and isolation assertions | full anatomy/development/lifespan | phases 1–6 scenario-driven packages |
| Neko morphotype | independent schema root | ear/tail anatomy and hearing/balance/thermal bindings | minimal fixture and isolation assertions | feline-human hybrid empirical reference ranges | focused expert estimates, then measured/calibrated replacements |
| cognition | proposal + disposition envelope | decision status/reasons and optional adopted goal/intention | accepted/rejected/deferred fixtures | neural gate dynamics and real provider behavior | Phase 1 scripted cortex; Phase 6 neurobiology; shadow launch real LLM |
| canonical time/replay | contract design + causal-kernel slice | transition stream, state hash, conservation report, simulated clock, idempotent receipts | `makise-causal-kernel` tests: quantities, admission, thermal conservation, partition invariance, restart/replay parity, legacy rejection, durable `events()` pagination, chain-tamper rejection, worker invariance 1/16 | full audit replay over archived exact artifact bytes still pending | Phase 2 physics mechanisms inherit the same replay matrix |
| model improvement | external control-plane design only | candidate digest, evidence, activation old/new digests, rollback | ADR/protocol/invariant contract only | candidate generation, evaluation automation, operational approval | post-V1 research; no autonomous production activation in V1 |
| physical apartment | legacy anchors only, non-normative for new state; optional metric room/object bounds and anchor positions in metres with runtime projection readers | metric bounds `{value, unit}` with unit/dimension rejection and semantic hash sensitivity; rigid-body mass (mg), pose (nm, y-up), centre-of-mass offset, principal inertia (mg·m²), angular velocity (µrad/s), momentum (mg·nm/s; mg·m²·µrad/s), energies (nJ); contact normal ±unit axis, penetration nm; impulse mg·nm/s; restitution and Coulomb µ micro fixed point [0,1]; tangential friction impulse mg·nm/s; environment floor plane y=0 and per-island rest qualification; balance verdict (Airborne/Stable/Tipping) over ≤2 support contacts with exact signed-area anchor; durable walk `ControlEpisode` (phase, target constraint, observed progress, reevaluation boundary, last blocker) proposing per-second gait transitions over fresh observables; fluid statics over declared fresh-water pool (ρ = 998_200_000 mg/m³ measured [CRC, 20 °C]): hydrostatic column nPa, buoyant force mg·nm/s², exact flotation verdict; liquid accounting — `LiquidContainer` capacity/content mm³, `pour_into` with exact conservation and first-class rim overflow (spill), puddle depth V/A over level floor footprint; room-atmosphere Compartment — measured dry-air ρ = 1_204_000 mg/m³ [CRC, 20 °C 1 atm] and c_v = 718 J/(kg·K) derived thermal reservoir per declared volume, temperature validity band −10..70 °C with atomic admit/reject heating, convective conductance G = h·A µJ/(mK·s) over declared free-convection h [Incropera band 5–25, expert_estimate midpoint], burner power→µJ conversion, vapour inventory with exact liquid↔vapour mass bridge via measured water density and absolute-humidity projection µg/m³; point-source field propagation — shared inverse-square kernel with sources rated at the declared 1 m calibration distance: sound intensity fW/m² with measured hearing-threshold constant [ISO 226], illuminance mlx by the π-free photometric law E = I_v/d² from candela ratings, odour mg/m³ as an `expert_estimate` still-air surrogate; per-modality validity bands and free-field assumption (no reflections/wind) | `makise-causal-kernel` tests: metric geometry legacy readability + unit rejection (`world/tests/metric_geometry.rs`); absolute hand-derived E = mgh / ½mv² / ½Iω² with exact work–energy identity ΔE = τ·Δθ on interval boundaries (`tests/rigid_body.rs`, `tests/articulation.rs`); deterministic island grouping, forward/reverse/flat schedule parity, two-worker partition merge bit-exact, explicit suspend/resume digest transitions (`tests/physics_island.rs`); textbook collision anchors — equal-mass exchange, plastic stop, 20/10 kg elastic 90 J↔90 J, partial-restitution loss, mass-split correction clearing overlap (`tests/collision.rs`); Coulomb friction anchors — stick at µ·J_n ≥ μ_r·Δv_t with exact common slip velocity and 45 J dissipation, boundary µ = 375000 sticking bit-exact, slide at µ = 0.25 shrinking slip 3→1 m/s without reversal (KE 170→130 J), momentum conserved bit-exact on all axes, µ > 1 rejected, two-axis slip typed `OutsideValidityRange`, frictionless default untouched (`tests/friction.rs`); physical rest trigger — pure `resting_islands` proposal over declared floor plane y = 0 plus intra-island vertical contacts: floor cube qualifies, 1 nm clearance or lateral drift disqualifies, two-cube stack rests through member contact, floating touching pair rejected, partially supported island rejected as a whole, deterministic under repetition (`tests/island_rest.rs`); bipedal balance anchors — point base needs exact COM alignment (1 nm lean tips), two-foot segment base via collinearity + axis bounds (centred stance stable, boundary foot stable, beyond-foot and lateral offsets tip), empty set Airborne, three contacts typed `OutsideValidityRange`, diagonal stances affine in negative coordinates (`tests/balance.rs`); walk ControlEpisode anchors — canonical nine-motion gait to an observed completion at the reevaluation boundary (max-axis proportional shifts, stride-ahead landings keep separation constant), unreachable target yields honest partial results forever, mid-swing perturbation blocks with recorded `BalanceLost` over an unchanged episode, replanned episode recovers to the identical completion coordinate, already-centred shift flips to swing zero-delta, fractional shift share typed `NonRepresentableShift`, non-positive targets rejected, stepping pure under repetition (`tests/walk_control.rs`); hydrostatics anchors — 1 m water column = 9_788_998_030_000 nPa ≈ 9789 Pa hand-multiplied from P = ρ·g·h, exact depth linearity (×2 and ×0.1), one submerged litre displaces 9_788_998_030_000_000 mg·nm/s² ≈ 9.789 N, wood floats / iron sinks by displaced-mass cross-multiplication, litre body of exactly 998_200 mg neutrally buoyant at the integer boundary (+1 mg sinks), degenerate inputs typed, fractional column typed `NonRepresentablePressure` (`tests/fluids.rs`); pour/spill anchors — clean 300 ml transfer with zero spill and bit-exact volume conservation, 700 ml request into 200 ml free space spills exactly 500 ml as a first-class outcome, request above available pours only what is held, request equal to free space fills exactly at the boundary, 250 ml over 0.25 m² stands exactly 1 mm deep, fractional quotient typed `NonRepresentableDepth`, degenerate containers/self-pour typed, pouring pure under repetition (`tests/liquid_pour.rs`); atmosphere anchors — 40 m³ room holds hand-multiplied 48_160_000 mg of air, one kelvin costs m·c_v·ΔT = 34_578.88 J with symmetric bit-exact restore, 2 kW×60 s = 120 kJ exact, G = 10 W/(m²·K) × 0.04 m² = 400 µJ/(mK·s), hot pot hands exactly G·ΔT = 32 J to the room through the shared thermal port with conserved deltas, organism ambient surrogate couples through the same port (100 J/s at ΔT = 2 K), 50 ml spill evaporates into exactly 49_910 mg vapour and absolute humidity 1_247_750 µg/m³, fractional volumes/masses/capacities/conductances/humidity typed, heating past the envelope rejected without partial application (`tests/atmosphere.rs`); propagation anchors — double distance quarters the field and the calibration distance returns the rating, 1 W/m² alarm reads 0.25 W/m² at 2 m and 0.01 W/m² at 10 m, 4000 fW/m² source lands exactly on the measured 1000 fW/m² hearing threshold at 2 m, one candela at one metre is exactly 1000 mlx, 100 cd bulb lights a desk at 5 m with 4000 mlx, odour surrogate dilutes 100→4 mg/m³ over 5 m, per-modality bands typed (40 m shout rejected while 40 m light passes), degenerate distances parameter errors (`tests/propagation.rs`) | collision response is COM-level without angular coupling; bodies are not yet persisted through `WorldEngine::commit`; walk episode drives foot/COM kinematics only, joint-level torque coupling to foot placement is outside the declared envelope; atmosphere evaporation is mass-only — latent heat and a temperature-dependent saturation curve are declared gaps, gas composition is a dry-air surrogate; propagation is free-field inverse-square — reflections, occlusion, wind and advection are declared gaps, odour transport is an expert_estimate surrogate not diffusion physics | Phase 2 remaining: electricity/water slice, `apartment-v2` acceptance scenario |
| locomotion/articulation | morphotype anatomy edges carry optional declared joints (`limit_min_urad`, `limit_max_urad`, `driven_inertia_mgm2`) behind an additive schema envelope | joint limits µrad, segment inertia mg·m², torque nJ/µrad (= 1e-3 N·m), per-joint angle/angular velocity; typed `JointLimitExceeded`/`NonRepresentableStep` without clamping | human fixture binds four leg joints, neko five including tail base through one data-driven code path; neutral-pose validity, empty-skeleton rejection, prime-inertia representability rejection (`tests/anatomy_binding.rs`, `tests/articulation.rs`) | balance feedback, perception coupling, motor ControlEpisodes, engine persistence of skeletons; joint limits are expert_estimate (human) and fictional species_proxy (neko), uncalibrated | Phase 2 walk slice; Phase 3 musculoskeletal physiology; measured ROM calibration per ADR-0014 |
| disease/injury/drugs/death | not implemented | contract families named only | long-horizon/rare-event plan | all biological parameters and solvers | Phase 4 |
| reproduction/development/aging | not implemented | lifecycle events named only | horizon definitions | compatibility, fetal and lifespan calibration | Phase 5 |
| capacity/scaling | schema has no entity cap | declared compute estimates and typed capacity failure | schema checks | actual workstation capacity | Phase 8 sweep to `CapacityExceeded` |

## Gate interpretation

`schema fixture` означает structural contract example, не работающий biological mechanism. В Phase 0 нет численного solver validation. Любая строка без runtime evidence остаётся unknown до указанной фазы и не может использоваться для claim о biological realism.
